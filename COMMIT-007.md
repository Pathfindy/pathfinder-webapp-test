# Commit 7 – Dashboard-Berechnung

Commit-Message:

`feat(dashboard): display calculated bonus totals`

Geänderte Dateien:

- `js/app.js`
- `js/berechnung.js`

Enthalten:

- Dashboard zeigt die berechneten Summen aller aktiven Effekte.
- Nicht belegte Ziele zeigen `0`.
- Positive Werte werden mit `+` dargestellt.
- Neuberechnung erfolgt nach Laden, Aktivieren/Deaktivieren, Speichern und Löschen.
- `berechneWerte()` gibt das Ergebnis weiterhin für Konsolentests zurück.
